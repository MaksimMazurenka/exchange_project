<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.finalproject">
    
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.FinalProject"
        tools:targetApi="m">
        <activity android:name=".activity.offer.GoodOffer"></activity>
        <activity android:name=".activity.offer.UserProductOffer" />
        <activity android:name=".activity.offer.UserOffers" />
        <activity android:name=".activity.market.NewDeal" />
        <activity android:name=".activity.market.ProductWithDeals" />
        <activity android:name=".activity.market.Market" />
        <activity android:name=".activity.user.AddProduct" />
        <activity android:name=".activity.user.MainWindow" />
        <activity android:name=".activity.login.Register" />
        <activity android:name=".activity.login.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>